import {Locale} from '@/i18n/config';import Hero from '@/components/Hero';import CategoryChips from '@/components/CategoryChips';import MarketingGrid from '@/components/MarketingGrid';import {getMessages} from '@/i18n/getMessages';export default async function Home({params}:{params:Promise<{locale:Locale}>}){const {locale}=await params;const m=await getMessages(locale);return (<div><Hero locale={locale}/><section><h2>{m.browse_marketing as string}</h2><p>{m.home_teaser as string}</p><MarketingGrid locale={locale}/></section><CategoryChips locale={locale}/></div>);}
