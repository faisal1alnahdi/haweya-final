import {getMessages} from '@/i18n/getMessages';import {Locale} from '@/i18n/config';export default async function Hero({locale}:{locale:Locale}){const m=await getMessages(locale);return (<section><h1>{m.hero_title as string}</h1><p>{m.hero_sub as string}</p><a href={`/${locale}/auth/login?role=client`}>{m.cta_client as string}</a> | <a href={`/${locale}/auth/login?role=supplier`}>{m.cta_supplier as string}</a></section>);}
